#!/bin/env sh

repo=$(find ~/.dotfiles ~/git -type d -name .git | cut -c "$(echo "$HOME/" | wc -c)-" | xargs -n1 dirname | rofi -dmenu -p "Repository") && $TERMINAL -d "$HOME/$repo" -e "$EDITOR" -c NvimTreeToggle -c RooterToggle
