#!/bin/env sh

repo=$(find ~/git -type d -name .git | cut -c "$(echo "$HOME/git/" | wc -c)-" | xargs -n1 dirname | rofi -dmenu -p "Repository") && $TERMINAL -d "$HOME/git/$repo" -e "$EDITOR" -c NvimTreeToggle
