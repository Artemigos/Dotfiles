{
    "prompt": "option",
    "commands": [
        {
            "description": "Update list",
            "command": "$TERMINAL -e sudo apt update"
        },
        {
            "description": "Upgrade",
            "command": "$TERMINAL -e sudo apt upgrade"
        }
    ]
}
